{
  "name": "routecastle-app",
  "version": "1.0.0",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "prebuild": "expo export --platform android",
    "test": "jest"
  },
  "dependencies": {
    "@expo/vector-icons": "^13.0.0",
    "@react-native/assets": "^1.0.0",
    // Remove @rnmapbox/maps if it's still there
    "expo": "~48.0.0",
    "expo-image-picker": "~14.1.1",
    "expo-splash-screen": "~0.18.0",
    "expo-status-bar": "~1.4.0",
    "react": "18.2.0",
    "react-native": "0.71.14",
    "react-native-maps": "1.3.2",
    "tesseract.js": "^5.0.0",
    // Add these two lines:
    "react-native-maps-osmdroid": "^1.0.0", // Ensure this version is compatible with your react-native-maps and RN versions
    "expo-build-properties": "~0.5.1" // Add this for native build customization
  },
  "devDependencies": {
    "@babel/core": "^7.20.0",
    "jest": "^29.0.0"
  }
}
